import React from './ReactAnswers.js';
import { message } from './message.js';

const ReactAnswer10 = () => {
    return(
    <div>
        <h1>{message}</h1>
    </div>
    )
}

export default ReactAnswer10;